{
	email {$CADDY_EMAIL}
}

# Public hostname (direct access + TLS).
scorer.ttacs.co.ke {
	encode zstd gzip
	reverse_proxy scorer:8091
}

# Vercel rewrites /mpesa-scorer/* to the Contabo IP (avoids scorer.* resolving to Vercel).
http://169.58.16.179 {
	encode zstd gzip
	reverse_proxy scorer:8091
}
